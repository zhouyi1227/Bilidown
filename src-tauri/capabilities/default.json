{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the local Bilidown application window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
